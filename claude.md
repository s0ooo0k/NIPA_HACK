# CultureBridge - 이주민 문화적응 & 정서지원 AI

## 프로젝트 개요

한국에 적응 중인 외국인/이주민을 위한 AI 기반 문화 갈등 해결 및 정서 지원 서비스

### 타겟 사용자

- 해외에서 오래 살다가 한국에 역이주한 교포/재외국민
- 한국 대학 외국인 유학생
- 한국 기업 외국인 직장인

## 기술 스택

- **Frontend**: React, Next.js (App Router)
- **Styling**: Tailwind CSS
- **AI 영상 생성**: OpenAI Sora API
- **AI 대화/분석**: OpenAI GPT-4o & GPT-4o-mini

## 핵심 기능

### 1. 대화형 상황 파악

사용자가 겪은 문화적 갈등 상황을 자연스러운 대화로 수집

```
[플로우]
사용자: "오늘 교수님이랑 이상한 일이 있었어..."
AI: "어떤 상황이었는지 편하게 얘기해줄래요? 🙂"
사용자: "밥 먹었어?라고 하셔서 점심 약속인 줄 알고 기다렸는데 아무도 안 왔어"
AI: "아 그랬구나... 혹시 그때 기분이 어땠어요?"
사용자: "되게 창피하고 화도 났어"
```

**구현 포인트**:

- 2-3턴의 자연스러운 대화로 맥락 수집
- 감정 상태 자연스럽게 파악 ("그때 기분이 어땠어요?")
- 음성/텍스트 입력 모두 지원

### 2. 감성 분석 + 솔루션 제시

대화 내용을 바탕으로 감정 분석 및 문화적 맥락 설명

```
[출력 예시]
📊 감정 분석
- 주요 감정: 당혹감, 수치심, 분노
- 상황 카테고리: 학교생활 > 교수 관계 > 언어/문화 오해

💡 이 상황 이해하기
'밥 먹었어?'는 한국에서 '안녕?' 같은 인사예요.
실제 식사 약속은 '내일 점심 같이 먹을래?' 처럼 구체적으로 말해요.

✅ 다음에 이렇게 해보세요
'네~ 먹었어요! 교수님은요?' 정도로 가볍게 답하면 OK
```

**구현 포인트**:

- 감정 추출: 당혹, 분노, 슬픔, 불안, 외로움 등
- 상황 카테고리 자동 분류
  - 학교생활 (수업, 교수관계, 조별과제)
  - 직장생활 (상사, 동료, 회식문화)
  - 일상생활 (집주인, 이웃, 행정처리)
  - 대인관계 (친구, 연인, 모임)
- 문화적 배경 설명 + 구체적 대응 가이드

### 3. AI 영상 시뮬레이션 (OpenAI Sora)

상황을 시각적으로 보여주는 AI 생성 영상

```
[영상 구성]
장면 1: 잘못된 이해 상황 재현
장면 2: 올바른 대응 방법 시연
장면 3: 비슷한 상황 비교 (진짜 약속 vs 인사)
```

**OpenAI Sora API 호출 예시** (API 공개 시):

```javascript
import OpenAI from "openai";

const openai = new OpenAI({ apiKey: process.env.OPENAI_API_KEY });

const generateSimulationVideo = async (scenario) => {
  // TODO: Sora API가 공개되면 실제 구현
  const response = await openai.videos.create({
    model: "sora-1.0",
    prompt: scenario.prompt,
    duration: 10,
    resolution: "1920x1080",
  });

  // 폴링으로 완료 대기
  while (response.status !== "completed") {
    await new Promise((resolve) => setTimeout(resolve, 5000));
    response = await openai.videos.retrieve(response.id);
  }

  return response;
};

// 사용 예시
const scenario = {
  prompt: `한국 대학교 복도에서 중년 남성 교수님이 외국인 학생에게
  '밥 먹었어?'라고 친근하게 인사하는 장면.
  학생이 자연스럽게 '네, 먹었어요! 교수님은요?'라고 미소 지으며 대답한다.
  배경: 밝은 대학교 복도, 자연광
  오디오: 자연스러운 한국어 대화, 복도 ambient 소리`,
};
```

### 4. 추가 학습 제안

비슷한 문화 차이 상황을 영상으로 추가 학습

```
[제안 예시]
AI: "비슷하게 헷갈릴 수 있는 표현들도 영상으로 볼까요? 😊

1. '다음에 보자' - 진짜 약속일까?
2. '어디 가?' - 왜 갑자기 물어보는 걸까?
3. '괜찮아' - 진짜 괜찮은 걸까?"

사용자: "1번!"
AI: [해당 상황 AI 영상 생성 및 재생]
```

## 프로젝트 구조

```
culturebridge/
├── app/
│   ├── page.tsx                 # 메인 채팅 인터페이스
│   ├── layout.tsx
│   ├── api/
│   │   ├── chat/route.ts        # 대화 처리 API
│   │   ├── analyze/route.ts     # 감성 분석 API
│   │   └── video/route.ts       # Sora 영상 생성 API
│   └── globals.css
├── components/
│   ├── ChatInterface.tsx        # 채팅 UI
│   ├── EmotionAnalysis.tsx      # 감정 분석 결과 표시
│   ├── SolutionCard.tsx         # 솔루션 카드
│   ├── VideoSimulation.tsx      # 영상 플레이어
│   └── LearningOptions.tsx      # 추가 학습 선택지
├── lib/
│   ├── openai.ts                # OpenAI API 클라이언트 (GPT-4o, Sora)
│   └── prompts.ts               # 프롬프트 템플릿
├── data/
│   └── scenarios.ts             # 문화 차이 시나리오 데이터
└── types/
    └── index.ts                 # 타입 정의
```

## 주요 시나리오 데이터

```typescript
// data/scenarios.ts
export const scenarios = {
  greetings: [
    {
      id: "bap-meogeosseo",
      korean: "밥 먹었어?",
      literal: "Did you eat?",
      actual: "How are you? / Hello",
      context: "한국에서 안부 인사로 자주 사용",
      correctResponse: "네~ 먹었어요! OO님은요?",
      videoPrompt:
        "한국 대학교 복도에서 교수님이 학생에게 '밥 먹었어?'라고 인사...",
    },
    {
      id: "daeume-boja",
      korean: "다음에 보자",
      literal: "Let's meet next time",
      actual: "Goodbye (not a real promise)",
      context: "헤어질 때 하는 인사, 구체적 약속이 아님",
      realPromise: "이번 주 토요일 어때? / 언제 시간 돼?",
      videoPrompt:
        "카페에서 친구들이 헤어지며 '다음에 보자~' 하고 손 흔드는 장면...",
    },
    {
      id: "eodi-ga",
      korean: "어디 가?",
      literal: "Where are you going?",
      actual: "Hello / Just checking in",
      context: "길에서 마주쳤을 때 하는 가벼운 인사",
      correctResponse: "아 그냥 잠깐요~ / 밥 먹으러요~",
      videoPrompt: "아파트 엘리베이터에서 이웃이 '어디 가?' 하고 묻는 장면...",
    },
  ],
  workplace: [
    {
      id: "geomto-halgeyo",
      korean: "검토해볼게요",
      literal: "I'll review it",
      actual: "Probably no / Soft rejection",
      context: "직장에서 완곡한 거절 표현",
      signs: ["구체적 피드백 없음", "후속 연락 없음"],
      videoPrompt:
        "회의실에서 상사가 제안서를 보며 '검토해볼게요' 하는 장면...",
    },
    {
      id: "hoesik-sul",
      korean: "한 잔만 해요",
      literal: "Just one drink",
      actual: "Social pressure to drink",
      context: "회식에서 술 권유, 거절이 어려운 상황",
      politeRefusal: ["건강/약 때문에요", "오늘 운전해야 해서요"],
      videoPrompt:
        "회식 자리에서 선배가 술을 권하고 후배가 정중히 거절하는 장면...",
    },
  ],
  school: [
    {
      id: "jobyul-gwaje",
      korean: "조별과제 역할 분담",
      context: "한국 대학의 조별과제 문화",
      tips: ["카톡 단톡방 필수", "역할 분담 명확히", "무임승차 주의"],
      videoPrompt: "대학 도서관에서 조별과제 회의하며 역할 나누는 장면...",
    },
  ],
};
```

## 환경 변수

```env
# .env.local
OPENAI_API_KEY=your_openai_api_key
```

## 개발 가이드

### AI 대화 프롬프트 가이드라인

- 따뜻하고 공감적인 톤 유지
- 2-3턴 대화로 충분한 맥락 수집
- 감정을 인정하고 공감 먼저, 솔루션은 그 다음
- 한국 문화를 비판하지 않고 객관적으로 설명

### 영상 프롬프트 가이드라인

- 구체적인 장소, 인물, 행동 묘사
- 오디오 지시 포함 (대화, 효과음, 배경음)
- 자연스러운 한국어 대화
- 8초 내에 핵심 상황 전달

### 감성 분석 카테고리

```typescript
type Emotion =
  | "confusion" // 혼란
  | "embarrassment" // 당혹/수치
  | "frustration" // 좌절
  | "anger" // 분노
  | "sadness" // 슬픔
  | "loneliness" // 외로움
  | "anxiety"; // 불안

type Category =
  | "school" // 학교생활
  | "workplace" // 직장생활
  | "daily" // 일상생활
  | "relationship"; // 대인관계
```

## 참고 자료

- [OpenAI API Docs](https://platform.openai.com/docs)
- [OpenAI Sora](https://openai.com/sora)
- [Next.js App Router](https://nextjs.org/docs/app)
